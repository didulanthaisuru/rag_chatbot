def process_transaction(transaction_details: str) -> str:
    """Processes a user's transaction and stores details."""
    # For now, just return the transaction details as confirmation
    return f"Transaction recorded: {transaction_details}"
